# `/private/`: широкоформатная адаптивная сетка

Дата публикации: 24 августа 2026 года.

## Изменение

В `/local/templates/solargy/assets/css/custom.css` добавлено отдельное правило для экранов шириной от `1440 px`:

- контейнер меню и основные контейнеры `/private/` расширяются до `1760 px`;
- горизонтальные поля рассчитываются относительно ширины окна и ограничены безопасным диапазоном;
- содержимое главного баннера расширено без чрезмерной длины строк;
- вводный текст ограничен шириной `1320 px` для читаемости;
- схема из трёх этапов и CTA расширяются до `1560 px`;
- заголовок преимуществ расширен до `1100 px`;
- правила ниже `1440 px` не изменялись.

## Резервная копия

До правки исходный CSS сохранён в репозитории:

- `backups/2026-08-24-private-wide-layout/custom.css.before.css`;
- размер `73 475` байт;
- SHA-256 `B6AF680DC28EDED8EF21639429604793A264B8DA4311065D89390DE08045AB67`;
- коммит резервной копии `568d71c`;
- ветка `codex/private-wide-backup`.

Инструкция отката находится рядом с резервной копией.

## Проверка после публикации

- `1920 px`: меню и секции `1760 px`, схема `1560 px`, горизонтального переполнения нет;
- `1440 px`: контейнеры `1353 px`, меню остаётся в две строки без наложений;
- `1024 px`: действующая промежуточная компоновка сохранена, переполнения нет;
- `390 px`: преимущества и проекты складываются в одну колонку, переполнения нет;
- промо, калькулятор и форма не изменялись и тестовые заявки не отправлялись;
- единственное предупреждение консоли относится к ранее известному пустому API-ключу Яндекс Карт.

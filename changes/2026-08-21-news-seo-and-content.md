# 2026-08-21 — исправление новостей и метаданных

- Статус: выполнено и проверено
- Где изменено: административная часть 1С-Битрикс, инфоблок «Новости» ID `7`, шаблон детальной новости
- Затронутые сущности: рубрики новостей, элементы ID `28`, `107`, `160`, `170`, `392`, `393`
- Причина: реальные дубли описаний, общие метатеги отдельных рубрик и две активированные статьи, которые выводились пустыми из-за специального устаревшего шаблона.

## Что изменено

- Для разделов инфоблока применены шаблоны самостоятельных `title`, `description` и заголовка страницы на основе названия рубрики.
- Исправлены анонс и SEO-метаданные материала ID `107` о естественном свете в медицинских учреждениях.
- Исправлены ручные SEO-метаданные ID `28`, `160` и `170`; скопированные описания заменены содержательными и соответствующими теме.
- Элементы ID `392` и `393` включены и наполнены самостоятельными материалами для Казахстана: сравнение световодов с мансардными окнами и зенитными фонарями.
- Для этих двух материалов добавлены анонсы и уникальные SEO-метаданные, убраны абсолютные неподтверждённые обещания, добавлено указание на расчёт КЕО и нормы РК.
- В шаблоне детальной новости отключено исключение для ID `392` и `393`, которое подменяло управляемый текст Bitrix устаревшим файлом `/include/news_custom.php`.

## Как проверено

- Проверены 8 публичных рубрик: у каждой самостоятельные метатеги и canonical; явно заданные старые значения разделов не затёрты шаблоном.
- Проверены 6 исправленных карточек: корректные H1, `title`, `description`, canonical и текст.
- Выполнен контрольный обход 50 URL новостей: 50 из 50 имеют H1, `title`, `description` и canonical; повторяющихся заголовков и описаний в проверенной выборке нет; сообщений «Элемент не найден» нет.
- Две восстановленные сравнительные статьи отображают содержательный текст и подзаголовки; PHP-ошибок нет.

## Что проверить после обхода роботом

- Обновление групп дублей в Яндекс Вебмастере после повторного обхода.
- Появление двух восстановленных URL в поисковой базе.
- При будущей публикации соблюдать `guides/news-seo-publishing.md`.

# Mail template 20 width backup

Original HTML of Bitrix mail template `#20` before increasing the desktop message width from 620 px to 800 px.

Rollback: restore `message-20.before.html` into the `MESSAGE` field of template `#20`.
